/*! @license
 * Shaka Player
 * Copyright 2016 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */


/**
 * @externs
 */


/**
 * Values must come from the shaka.config.CodecSwitchingStrategy runtime enum.
 *
 * TODO(TS): Restore as a literal union type when migrating to TypeScript.
 *
 * @typedef {string}
 */
shaka.extern.CodecSwitchingStrategy;


/**
 * Values must come from the shaka.config.CrossBoundaryStrategy runtime enum.
 *
 * TODO(TS): Restore as a literal union type when migrating to TypeScript.
 *
 * @typedef {string}
 */
shaka.extern.CrossBoundaryStrategy;


/**
 * Values must come from the shaka.config.MsfVersion runtime enum.
 *
 * TODO(TS): Restore as a literal union type when migrating to TypeScript.
 *
 * @typedef {string}
 */
shaka.extern.MsfVersion;


/**
 * Values must come from the shaka.config.MsfFilterType runtime enum.
 *
 * TODO(TS): Restore as a literal union type when migrating to TypeScript.
 *
 * @typedef {number}
 */
shaka.extern.MsfFilterType;


/**
 * Values must come from the shaka.config.PositionArea runtime enum.
 *
 * TODO(TS): Restore as a literal union type when migrating to TypeScript.
 *
 * @typedef {number}
 */
shaka.extern.PositionArea;


/**
 * Values must come from the shaka.config.RepeatMode runtime enum.
 *
 * TODO(TS): Restore as a literal union type when migrating to TypeScript.
 *
 * @typedef {number}
 */
shaka.extern.RepeatMode;
